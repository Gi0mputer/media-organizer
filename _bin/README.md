# Binary Tools Folder

Place the following executables in this folder to make them available to all scripts in this repository without needing to install them system-wide:

- `ffmpeg.exe`
- `ffprobe.exe`
- `exiftool.exe` (rename from `exiftool(-k).exe` if needed)

You can run `..\setup_tools.ps1` to automatically download these tools.
